{
  "name": "cti-enrichment-gateway",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "bootstrap": "bash scripts/bootstrap-linux.sh",
    "verify:tooling": "bash scripts/verify-tooling.sh",
    "lint:shell": "shellcheck scripts/*.sh",
    "check": "bash -n scripts/*.sh && npm run lint:shell && npm test",
    "test": "node --test"
  },
  "engines": {
    "node": ">=20"
  }
}
