export const GATEWAY_VERSION = '2.0.0';
export const EVIDENCE_SCHEMA_VERSION = '2.0';

export const VERSION_INFO = Object.freeze({
  gatewayVersion: GATEWAY_VERSION,
  schemaVersion: EVIDENCE_SCHEMA_VERSION,
});
