# No-secret-return policy

No API route may return, list, log or transform the value of any environment variable used as a credential. Health/configuration surfaces may expose only a boolean indicating whether a named credential is configured.
