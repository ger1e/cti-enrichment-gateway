# Provider health

Each enrichment response records per-provider state such as ok, timeout, rate_limited, http_error, provider_error or unsupported. Provider degradation must not erase successful evidence from other sources.
