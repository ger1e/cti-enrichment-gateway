# Reproducibility

Every provider result records retrieved time, parser version and a SHA-256 hash of the normalized provider payload. Important investigations should later add durable snapshots and gateway commit identifiers so conclusions can be reconstructed after upstream data changes.
